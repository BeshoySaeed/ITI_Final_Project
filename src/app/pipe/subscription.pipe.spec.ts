import { SubscriptionPipe } from './subscription.pipe';

describe('SubscriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SubscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
